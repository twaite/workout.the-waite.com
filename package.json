{
  "name": "main",
  "version": "1.0.50",
  "module": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile src/index.ts --outfile workout-app",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix"
  },
  "dependencies": {
    "@elysiajs/html": "^1.4.0",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "oxfmt": "^0.23.0",
    "oxlint": "^1.38.0"
  }
}